# libretranslate

Portainer-stack `libretranslate` (id 9, endpoint "primary") — zelf-gehoste
LibreTranslate-instantie, alleen bereikbaar op het LAN (`192.168.10.5:5000`).

Gemigreerd van file-based Portainer-deployment naar Git-gebaseerde deployment
zodat wijzigingen voortaan traceerbaar en terug te draaien zijn.

Geen geheimen in deze stack.
